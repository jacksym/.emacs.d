#include <iostream> 
#include <string>
  
  
// main function -
// where the execution of program begins
int main()
{
    // prints hello world
    // char greeting[] = "hello everybody, my name is Jack";
    // std::string greeting = "hi I'm jack";
    std::string greeting = "when in the course of human events...";
    std::cout << greeting << std::endl;

    return 0;
}
